# 表结构问答清单

> 用途：这份文档用于和你本地数据库做人工比对。
> 依据：你提供的真实数据库表结构，并结合 `backend/app/models.py`、`backend/seed_health.sql`、`backend/app/database.py` 做备注说明。
> 建议：优先以这份文档中的“实际数据库结构”为准，再去核对当前代码期望的结构。

> 2026-03-31 增量更新（本次代码改动）：
> - 新增 `users.encryption_public_key`（用户加密公钥）
> - 新增 `health_data_user.owner_encrypted_dek`、`health_data_user.encryption_version`
> - 新增表 `health_data_grants`（健康数据授权关系与被授权 DEK 包装）

---

## 问：这份文档现在记录的是什么？

| 问题 | 回答 |
| --- | --- |
| 这份文档基于什么整理？ | 基于你刚刚提供的 **真实数据库表结构** 整理，不再只是代码推断。 |
| 这份文档适合做什么？ | 适合你后续人工核对数据库、对照代码、排查旧表残留和结构差异。 |
| 是否包含旧表？ | 包含。比如 `health_data` 也保留说明，因为它实际存在于你的数据库里。 |

---

## 问：当前数据库里有哪些表？

| 表名 | 当前状态 | 说明 |
| --- | --- | --- |
| `article_favorites` | 存在 | 文章收藏关系表 |
| `article_read_histories` | 存在 | 文章阅读历史表 |
| `chat_messages` | 存在 | AI 聊天记录表 |
| `health_articles` | 存在 | 健康文章表 |
| `health_data` | 存在 | 旧版健康数据表 |
| `health_data_user` | 存在 | 当前健康数据主表 |
| `health_data_grants` | 新增 | 健康数据授权关系表（DEK 包装分发） |
| `rag_knowledge_chunks` | 存在 | RAG 分块元数据表 |
| `rag_knowledge_documents` | 存在 | RAG 文档表 |
| `roles` | 存在 | 角色表 |
| `system_logs` | 存在 | 系统日志表 |
| `system_settings` | 存在 | 系统设置表 |
| `users` | 存在 | 用户主表 |

---

## 问：哪些表最值得优先比对？

| 优先级 | 表名 | 原因 |
| --- | --- | --- |
| 高 | `users` | 字段最多，历史迭代痕迹最明显 |
| 高 | `health_data_user` | 涉及加密、文件、公开私密、链上字段 |
| 高 | `chat_messages` | 有 `session_id`，关系到 AI 会话分组 |
| 高 | `rag_knowledge_documents` | RAG 主表 |
| 高 | `rag_knowledge_chunks` | 向量分块表 |
| 中 | `health_data` | 它是旧表，但数据库里仍然存在 |

---

## 问：`article_favorites` 表现在是什么结构？

| 字段名 | 类型 | 可空 | 键 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `id` | `int` | `NO` | `PRI` | `auto_increment` | 主键 |
| `user_id` | `int` | `NO` | `MUL` |  | 用户 ID |
| `article_id` | `int` | `NO` | `MUL` |  | 文章 ID |
| `created_at` | `datetime` | `NO` |  | `CURRENT_TIMESTAMP` | 收藏时间 |

### 备注

| 项目 | 说明 |
| --- | --- |
| 表用途 | 记录用户收藏文章的关系 |
| 应关注点 | 你给出的结构里没有直接展示联合唯一约束，若要进一步确认，建议再查索引信息 |

---

## 问：`article_read_histories` 表现在是什么结构？

| 字段名 | 类型 | 可空 | 键 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `id` | `int` | `NO` | `PRI` | `auto_increment` | 主键 |
| `user_id` | `int` | `NO` | `MUL` |  | 用户 ID |
| `article_id` | `int` | `NO` | `MUL` |  | 文章 ID |
| `read_count` | `int` | `NO` |  |  | 阅读次数 |
| `last_read_at` | `datetime` | `NO` |  | `CURRENT_TIMESTAMP` | 最后阅读时间 |

### 备注

| 项目 | 说明 |
| --- | --- |
| 表用途 | 记录用户对文章的阅读行为 |
| 应关注点 | 若项目逻辑依赖“用户+文章唯一”，建议再查唯一索引是否存在 |

---

## 问：`chat_messages` 表现在是什么结构？

| 字段名 | 类型 | 可空 | 键 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `id` | `int` | `NO` | `PRI` | `auto_increment` | 主键 |
| `user_id` | `int` | `NO` | `MUL` |  | 所属用户 |
| `message` | `text` | `NO` |  |  | 消息内容 |
| `is_user` | `tinyint(1)` | `NO` |  |  | 是否为用户消息 |
| `created_at` | `datetime` | `NO` |  | `now()` | 创建时间 |
| `session_id` | `int` | `YES` |  |  | 会话分组 ID |

### 备注

| 项目 | 说明 |
| --- | --- |
| 表用途 | 存 AI 聊天消息 |
| 结构状态 | 已包含 `session_id`，说明你的数据库已升级到按会话分组的版本 |
| 和代码关系 | 与当前后端代码期望一致 |

---

## 问：`health_articles` 表现在是什么结构？

| 字段名 | 类型 | 可空 | 键 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `id` | `int` | `NO` | `PRI` | `auto_increment` | 主键 |
| `title` | `varchar(200)` | `NO` | `MUL` |  | 标题 |
| `category` | `varchar(50)` | `NO` | `MUL` |  | 分类 |
| `summary` | `varchar(500)` | `YES` |  |  | 摘要 |
| `content` | `text` | `NO` |  |  | 正文 |
| `cover_image` | `varchar(500)` | `YES` |  |  | 封面图 |
| `tags` | `varchar(500)` | `YES` |  |  | 标签 |
| `view_count` | `int` | `NO` | `MUL` |  | 浏览量 |
| `created_at` | `datetime` | `NO` |  | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NO` |  | `CURRENT_TIMESTAMP` | 更新时间 |

### 备注

| 项目 | 说明 |
| --- | --- |
| 表用途 | 健康知识文章主表 |
| 和代码关系 | 与当前 ORM 基本一致 |

---

## 问：`health_data` 表现在是什么结构？

| 字段名 | 类型 | 可空 | 键 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `id` | `int` | `NO` | `PRI` | `auto_increment` | 主键 |
| `user_id` | `int` | `NO` | `MUL` |  | 用户 ID |
| `weight` | `float` | `YES` |  |  | 体重 |
| `height` | `float` | `YES` |  |  | 身高 |
| `blood_pressure_systolic` | `int` | `YES` |  |  | 收缩压 |
| `blood_pressure_diastolic` | `int` | `YES` |  |  | 舒张压 |
| `heart_rate` | `int` | `YES` |  |  | 心率 |
| `blood_sugar` | `float` | `YES` |  |  | 血糖 |
| `recorded_at` | `datetime` | `NO` |  | `now()` | 记录时间 |
| `is_private` | `tinyint(1)` | `NO` |  | `0` | 是否私密 |
| `health_data_image` | `text` | `YES` |  |  | 图片 |
| `record_type` | `varchar(20)` | `NO` |  | `manual` | 记录类型 |
| `health_data_file_name` | `varchar(255)` | `YES` |  |  | 文件名 |
| `health_data_file` | `text` | `YES` |  |  | 文件内容或路径 |

### 备注

| 项目 | 说明 |
| --- | --- |
| 表用途 | 这是旧版健康数据表，偏向结构化指标录入 |
| 和当前代码关系 | **当前后端 ORM 主用的是 `health_data_user`，不是这张表** |
| 结论 | 这张表大概率属于历史遗留表，建议你确认当前业务是否还在使用 |

---

## 问：`health_data_user` 表现在是什么结构？

| 字段名 | 类型 | 可空 | 键 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `id` | `int` | `NO` | `PRI` | `auto_increment` | 主键 |
| `user_id` | `int` | `NO` | `MUL` |  | 用户 ID |
| `data_title` | `varchar(255)` | `YES` |  |  | 数据标题 |
| `data_content` | `longtext` | `YES` |  |  | 文本内容 |
| `pdf_data` | `longblob` | `YES` |  |  | PDF 二进制 |
| `file_type` | `varchar(20)` | `NO` |  | `text` | 文件类型 |
| `pdf_size` | `int` | `YES` |  |  | PDF 大小 |
| `created_at` | `timestamp` | `YES` |  | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `timestamp` | `YES` |  | `CURRENT_TIMESTAMP` | 更新时间 |
| `encrypted_data_content` | `text` | `YES` |  |  | 加密文本内容 |
| `encrypted_pdf_data` | `longblob` | `YES` |  |  | 加密 PDF 数据 |
| `owner_encrypted_dek` | `text` | `YES` |  |  | 所有者可解包的 DEK 包装 |
| `encryption_version` | `varchar(20)` | `NO` |  | `legacy` | 加密版本（`legacy` / `dek_v1`） |
| `is_public` | `tinyint(1)` | `NO` |  | `0` | 是否公开 |
| `onchain_data_id` | `varchar(66)` | `YES` |  |  | 链上数据 ID |
| `onchain_tx_hash` | `varchar(66)` | `YES` |  |  | 链上交易哈希 |
| `file_mime_type` | `varchar(100)` | `YES` |  |  | 文件 MIME 类型 |

### 备注

| 项目 | 说明 |
| --- | --- |
| 表用途 | 当前健康数据主表，支持文本/PDF、公开私密、加密、链上扩展 |
| 和代码关系 | 与当前 ORM 基本一致 |
| 加密升级 | 私密数据已支持 `DEK + 公钥包装`，历史数据仍兼容 `legacy` 解密路径 |
| 与旧表关系 | 这张表看起来是 `health_data` 的后续替代方案 |

---

## 问：`health_data_grants` 表现在是什么结构？

| 字段名 | 类型 | 可空 | 键 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `id` | `int` | `NO` | `PRI` | `auto_increment` | 主键 |
| `record_id` | `int` | `NO` | `MUL` |  | 健康记录 ID（关联 `health_data_user.id`） |
| `owner_user_id` | `int` | `NO` | `MUL` |  | 授权方用户 ID |
| `grantee_user_id` | `int` | `NO` | `MUL` |  | 被授权用户 ID |
| `wrapped_dek` | `text` | `NO` |  |  | 给被授权者公钥包装后的 DEK |
| `can_read` | `tinyint(1)` | `NO` |  | `1` | 是否可读 |
| `expires_at` | `datetime` | `YES` | `MUL` |  | 授权过期时间 |
| `remark` | `varchar(255)` | `YES` |  |  | 授权备注 |
| `revoked_at` | `datetime` | `YES` | `MUL` |  | 撤销时间（软撤销） |
| `created_at` | `datetime` | `NO` |  | `CURRENT_TIMESTAMP` | 创建时间 |

### 备注

| 项目 | 说明 |
| --- | --- |
| 表用途 | 记录每条健康数据对外授权关系，并保存被授权者可解包的 DEK 包装 |
| 安全意义 | 授权时无需共享所有者私钥，通过 DEK 包装实现可控解密 |
| 生命周期 | 可通过 `expires_at` 与 `revoked_at` 管控授权有效性 |

---

## 问：`rag_knowledge_chunks` 表现在是什么结构？

| 字段名 | 类型 | 可空 | 键 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `id` | `int` | `NO` | `PRI` | `auto_increment` | 主键 |
| `document_id` | `int` | `NO` | `MUL` |  | 所属文档 |
| `point_id` | `varchar(80)` | `NO` | `UNI` |  | 向量点 ID |
| `chunk_index` | `int` | `NO` |  |  | 分块序号 |
| `content` | `text` | `NO` |  |  | 分块内容 |
| `char_count` | `int` | `NO` |  |  | 字符数 |
| `is_active` | `tinyint(1)` | `NO` | `MUL` |  | 是否启用 |
| `created_at` | `datetime` | `NO` |  | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NO` |  | `CURRENT_TIMESTAMP` | 更新时间 |

### 备注

| 项目 | 说明 |
| --- | --- |
| 表用途 | 存 RAG 分块和向量索引关联信息 |
| 和代码关系 | 与当前 ORM 基本一致 |
| 应关注点 | ORM 里还定义了 `(document_id, chunk_index)` 组合唯一，建议你再查一次索引确认数据库中是否真的存在 |

---

## 问：`rag_knowledge_documents` 表现在是什么结构？

| 字段名 | 类型 | 可空 | 键 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `id` | `int` | `NO` | `PRI` | `auto_increment` | 主键 |
| `title` | `varchar(200)` | `NO` | `MUL` |  | 标题 |
| `category` | `varchar(50)` | `NO` | `MUL` |  | 分类 |
| `content` | `text` | `NO` |  |  | 正文 |
| `source` | `varchar(255)` | `YES` |  |  | 来源 |
| `tags` | `varchar(500)` | `YES` |  |  | 标签 |
| `is_active` | `tinyint(1)` | `NO` | `MUL` |  | 是否启用 |
| `created_at` | `datetime` | `NO` |  | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NO` |  | `CURRENT_TIMESTAMP` | 更新时间 |

### 备注

| 项目 | 说明 |
| --- | --- |
| 表用途 | RAG 文档主表 |
| 和代码关系 | 与当前 ORM 一致 |

---

## 问：`roles` 表现在是什么结构？

| 字段名 | 类型 | 可空 | 键 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `id` | `int` | `NO` | `PRI` | `auto_increment` | 主键 |
| `name` | `varchar(20)` | `NO` | `UNI` |  | 角色名 |
| `description` | `varchar(100)` | `YES` |  |  | 角色描述 |
| `created_at` | `datetime` | `NO` |  | `now()` | 创建时间 |

### 备注

| 项目 | 说明 |
| --- | --- |
| 表用途 | 角色定义表 |
| 和代码关系 | 与当前 ORM 一致 |

---

## 问：`system_logs` 表现在是什么结构？

| 字段名 | 类型 | 可空 | 键 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `id` | `int` | `NO` | `PRI` | `auto_increment` | 主键 |
| `level` | `varchar(20)` | `NO` | `MUL` |  | 日志级别 |
| `module` | `varchar(100)` | `NO` | `MUL` |  | 模块 |
| `action` | `varchar(100)` | `NO` |  |  | 操作名 |
| `message` | `text` | `NO` |  |  | 日志内容 |
| `operator_id` | `int` | `YES` | `MUL` |  | 操作人 |
| `created_at` | `datetime` | `NO` | `MUL` | `CURRENT_TIMESTAMP` | 创建时间 |

### 备注

| 项目 | 说明 |
| --- | --- |
| 表用途 | 系统操作日志 |
| 和代码关系 | 与当前 ORM 一致 |

---

## 问：`system_settings` 表现在是什么结构？

| 字段名 | 类型 | 可空 | 键 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `id` | `int` | `NO` | `PRI` | `auto_increment` | 主键 |
| `setting_key` | `varchar(100)` | `NO` | `UNI` |  | 设置键 |
| `setting_value` | `text` | `NO` |  |  | 设置值 |
| `updated_by` | `int` | `YES` | `MUL` |  | 更新人 |
| `created_at` | `datetime` | `NO` |  | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NO` |  | `CURRENT_TIMESTAMP` | 更新时间 |

### 备注

| 项目 | 说明 |
| --- | --- |
| 表用途 | 系统配置项 |
| 和代码关系 | 与当前 ORM 一致 |

---

## 问：`users` 表现在是什么结构？

| 字段名 | 类型 | 可空 | 键 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `id` | `int` | `NO` | `PRI` | `auto_increment` | 主键 |
| `username` | `varchar(50)` | `NO` | `UNI` |  | 用户名 |
| `email` | `varchar(255)` | `NO` | `UNI` |  | 邮箱 |
| `password_hash` | `varchar(255)` | `NO` |  |  | 密码哈希 |
| `role` | `varchar(20)` | `NO` |  |  | 角色字符串 |
| `is_active` | `tinyint(1)` | `NO` |  |  | 是否启用 |
| `created_at` | `datetime` | `NO` |  | `now()` | 创建时间 |
| `updated_at` | `datetime` | `NO` |  | `now()` | 更新时间 |
| `role_id` | `int` | `YES` | `MUL` |  | 角色 ID |
| `wallet_address` | `varchar(42)` | `YES` |  |  | 钱包地址 |
| `private_key_hash` | `varchar(128)` | `YES` |  |  | 私钥哈希 |
| `encrypted_profile_data` | `text` | `YES` |  |  | 加密资料 |
| `public_profile_data` | `text` | `YES` |  |  | 公开资料 |
| `profile_is_public` | `tinyint(1)` | `NO` |  | `0` | 资料是否公开 |
| `encrypted_private_key` | `text` | `YES` |  |  | 加密私钥 |
| `encryption_public_key` | `text` | `YES` |  |  | 用户加密公钥（用于 DEK 包装） |
| `social_provider` | `varchar(20)` | `YES` |  |  | 社交登录平台 |
| `social_open_id` | `varchar(128)` | `YES` |  |  | 社交平台标识 |
| `social_nickname` | `varchar(100)` | `YES` |  |  | 社交昵称 |
| `encrypted_email` | `text` | `YES` |  |  | 加密邮箱 |
| `email_hash` | `varchar(64)` | `YES` |  |  | 邮箱哈希 |
| `encrypted_wallet_address` | `text` | `YES` |  |  | 加密钱包地址 |
| `wallet_address_hash` | `varchar(64)` | `YES` |  |  | 钱包地址哈希 |
| `encrypted_social_open_id` | `text` | `YES` |  |  | 加密社交标识 |
| `social_open_id_hash` | `varchar(64)` | `YES` |  |  | 社交标识哈希 |
| `home_ai_advice_cache` | `text` | `YES` |  |  | 首页 AI 建议缓存 |

### 备注

| 项目 | 说明 |
| --- | --- |
| 表用途 | 用户主表，承载认证、角色、资料、钱包、社交登录、AI 缓存等能力 |
| 和代码关系 | 字段上与当前 ORM 基本一致 |
| 加密升级 | 新增 `encryption_public_key` 用于私密健康数据 DEK 分发授权 |
| 应关注点 | 你给出的字段列表里，`wallet_address`、`email_hash`、`wallet_address_hash`、`social_open_id_hash` 没显示 `UNI/MUL`，建议你再查索引确认数据库是否已建唯一索引 |

---

## 问：当前数据库结构和代码设计相比，最明显的差异是什么？

| 差异点 | 说明 |
| --- | --- |
| `health_data` 仍然存在 | 这是旧版健康数据表，但当前代码主用的是 `health_data_user` |
| `health_data_user` 已是新主表 | 说明你的数据库同时保留了旧设计和新设计 |
| `chat_messages.session_id` 已存在 | AI 会话分组字段已经落库 |
| RAG 两张表已存在 | 说明 RAG 相关结构已经实际建成 |
| 一些 ORM 中声明的唯一约束，字段清单里未完全体现 | 这不一定表示没有，只是 `SHOW COLUMNS` 看不全约束细节，最好再查索引 |

---

## 问：如果你下一步要继续比对，建议怎么查？

| 建议步骤 | 说明 |
| --- | --- |
| 查所有索引 | 用来确认唯一约束和联合索引是否真的存在 |
| 查外键 | 用来确认表关系是否完整 |
| 查 `health_data` 是否仍被业务使用 | 用来判断是不是可以视为遗留表 |

### 推荐 SQL

```sql
SHOW INDEX FROM users;
SHOW INDEX FROM article_favorites;
SHOW INDEX FROM article_read_histories;
SHOW INDEX FROM rag_knowledge_chunks;
```

```sql
SELECT TABLE_NAME, COLUMN_NAME, CONSTRAINT_NAME, REFERENCED_TABLE_NAME, REFERENCED_COLUMN_NAME
FROM information_schema.KEY_COLUMN_USAGE
WHERE TABLE_SCHEMA = DATABASE()
  AND REFERENCED_TABLE_NAME IS NOT NULL;
```

---

## 问：一句话总结当前数据库状态？

| 问题 | 回答 |
| --- | --- |
| 当前数据库是什么状态？ | 当前数据库已经包含新版本用户、健康数据、AI、RAG、系统管理相关结构，但同时还保留了一张旧版 `health_data` 表，说明库里存在历史演进痕迹。 |
